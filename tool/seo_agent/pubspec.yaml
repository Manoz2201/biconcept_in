name: seo_agent
description: Daily BiConcept SEO agent — SERP snippets, Gemini proposals, JSON writes.
publish_to: "none"

environment:
  sdk: ^3.13.0

dependencies:
  http: ^1.6.0
  path: ^1.9.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.3
