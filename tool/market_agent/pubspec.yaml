name: market_agent
description: Daily NCR market-research agent — Serper snippets, Gemini extraction, unpublished Appwrite listings.
publish_to: "none"

environment:
  sdk: ^3.13.0

dependencies:
  agent_runtime:
    path: ../agent_runtime
  crypto: ^3.0.6
  dart_appwrite: ^19.3.0
  http: ^1.6.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.3
